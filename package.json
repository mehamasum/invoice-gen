{
  "name": "invoice-gen",
  "version": "0.0.1",
  "description": "Generate PDF invoice and email it.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "setup": "cp sample.env .env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mehamasum",
  "license": "MIT",
  "dependencies": {
    "dayjs": "^1.11.3",
    "dotenv": "^16.0.1",
    "pdf-creator-node": "^2.3.4"
  }
}
